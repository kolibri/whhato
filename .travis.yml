language: php
php:
  - '7.2'

before_script:
  - composer self-update

install:
  - cd project
  - composer install
  - ./bin/phpunit install

script:
  - ./bin/console lint:yaml data
  - ./bin/phpunit
